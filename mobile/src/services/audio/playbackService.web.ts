/**
 * Web stub — react-native-track-player has no web runtime.
 * Loaded only on web; native builds use playbackService.rntp.ts.
 */
export async function playbackService(): Promise<void> {
  // no-op
}
